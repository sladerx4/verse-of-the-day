# Token de Vercel Blob (Vercel Dashboard → Storage → Blob → Create Store → .env.local)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxx

# Solo para desarrollo local del widget
NEXT_PUBLIC_APP_URL=http://localhost:3000
